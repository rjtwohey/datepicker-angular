import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'date-picker-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
